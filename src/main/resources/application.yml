spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:product-pricing-db;DB_CLOSE_ON_EXIT=FALSE
    username: sa
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true